
name: Unpack project
on:
  workflow_dispatch:
permissions:
  contents: write
  workflows: write
jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unzip
        run: |
          unzip -o 'local-llm-chat(1).zip' -d /tmp/proj
          cp -r /tmp/proj/local-llm-chat/. .
          rm -f 'local-llm-chat(1).zip'
      - name: Commit
        run: |
          git config user.name "nightlais22"
          git config user.email "nightlais22@users.noreply.github.com"
          git add -A
          git commit -m "unpack project" || echo "nothing to commit"
          git push
